import { styled } from "styled-components";

const StyledFooter = styled.footer`
  min-height: 1000px;
  text-align: center;
`;

const FooterImage = styled.img`
  left: 0;
  right: 0;
  bottom: 0;
`;

export const S = {
  StyledFooter,
  FooterImage,
};
